Leave the venv: deactivate

Original Directory: cd /Users/pradarshandahal/Desktop/Arbeit/prefshap_imp

Remove the venv: rm -rf .venv

Create new venv: python3.12 -m venv .venv

Activate the venv: source .venv/bin/activate

# pip install --upgrade pip

Install required packages from requirements.txt: pip install -r requirements.txt

# check:
# python -c "import numpy, torch, shapiq, xgboost; print('environment OK')"

# pip install --force-reinstall --no-cache-dir torch xgboost

Run the tests: 

python run_test.py
python run_shapiq_test.py

Pokemon:
python pokemon_data.py 
python fit_g_pokemon.py
python benchmark_pokemon.py
python test_seeds.py
python plot_test_seeds.py

# Case study 2:
python case_study_2.py
python plotting_study.py

# Aggregate pokemon benchmark:
python agg_pokemon.py \
    --n-duels 2 \
    --budgets 200 1000 2000 \
    --seeds 0 1 \
    --benchmark-all \
    --out-dir pokemon_benchmark_pilot

python agg_pokemon.py \
    --n-duels 20 \
    --budgets 200 1000 2000 \
    --seeds 0 1 2 \
    --benchmark-all \
    --out-dir pokemon_benchmark_pilot_2